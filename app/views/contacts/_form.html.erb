<%= form_with(model: contact) do |form| %>
  <div class="col-12 col-md-4">
  <% if contact.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(contact.errors.count, "error") %> prohibited this contact from being saved:</h2>

      <ul>
        <% contact.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :name,  :class=> "fw-semibold text-secondary mb-1" %><br>
    <%= form.text_field :name,:class=> "form-control" %>
  </div>

  <div class="form-group">
    <%= form.label :number,  :class=> "fw-semibold text-secondary mb-1"%><br>
    <%= form.text_field :number,:class=> "form-control" %><br>
  </div>
  <%= form.hidden_field :user_id, :value => current_user.id %>
  </div>
  <div>
    <%= form.submit :class=> "btn btn-primary btn-sm btn-block"%>
  </div>
<% end %>
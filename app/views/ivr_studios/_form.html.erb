<%= form_with(model: ivr_studio) do |form| %>
<div class="col-12 col-md-4">
  <% if ivr_studio.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(ivr_studio.errors.count, "error") %> prohibited this ivr_studio from being saved:</h2>

      <ul>
        <% ivr_studio.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div >
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name, required: true,:class => "form-control" %></br>
  </div>

  <div>
    <%= form.submit :class=>"btn btn-primary"%>
  </div>
  </div>
<% end %>
